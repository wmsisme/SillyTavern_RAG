---
章节: 第6章：打造高效正则表达式
章节ID: 06_打造高效正则表达式
切片序号: 62/74
---
直接的办法

在第5章( 196),我给出了匹配分隔符之内文本的公式：

1. 匹配起始分隔符；

2. 匹配正文：匹配“除结束分隔符之外的任何字符”;

3. 匹配结束分隔符。

现在我们的程序以/x 和x/ 作为开始和结束分隔符，它似乎很符合这个模式。难处在于匹配 “除结束分隔符之外的任何字符”。如果结束分隔符是单个字符，我们可以用排除型字符组。 但字符组不能用来进行多字符匹配，不过如果能使用否定型顺序环视，我们就能使用 (?:(?! x/).)*」。 这就是除结束分隔符之外的任何字符。

于是我们得到/x(?:(?! x/).)*x/J 。 它没有问题，但速度很慢(在我做的一些测试中，速 度要比下面的表达式慢几百倍)。这个思路很有用，但缺乏实用性，因为几乎所有支持顺序 环视的流派都支持忽略优先量词，所以效率并不是问题，你完全可以用/x.*?x/J。

那么,顺着这种分三步走的思路，是否有其他办法匹配第一个x1 之前的文本?能想到的办 法有两个。之一是把x 作为开始分隔符和结束分隔符，也就是说，匹配除 x 之外的任何字 符，以及之后字符不为斜线的x 。这样，“除结束分隔符之外的任何字符”就成了：

· 除 x 之外的任何字符：[^ x]J。

· 之后字符不是斜线的x: x[^/]J。

这样得到([^ x]Ix[^/])* 来匹配主体文本，/x([^x]I×[^/])*x/ 来匹配整个注释。我们 会发现这条路行不通。

274 第 6 章 ： 打 造 高 效 正 则 表 达 式

另一种办法是，把紧跟在 x 之后斜线当作结束分隔符。这样“除结束分隔符之外的任何字 符”就成了：

· 除斜线外的任何字符：[^/] J。

· 紧跟在x 之后的斜线：[^ x]/J。

于是用([^/]1[^x]/)*1 匹配主体文本，/ x([^/]I[^x]/)*x / 匹配整个注释。

不幸的是，这同样是死路。

如果用/ x([^x]lx[^/])*x/ 来匹配‘/xx·foo*xx/', 在 'foo · ’ 之后，第一个x由

匹配，这当然没有问题。但是之后，x i^/ 匹 配 xx/, 而这个 x 应该是标记注释的结束。

于是继续下一轮迭代，[^ x]: 匹配斜线，结果会匹配×1之后的文本。

/×[F^/)1^x]/)*×/1 也不能匹配‘/x/f·oo·/ x/' ( 整个注释都应该匹配)。如果注释结尾 后紧跟斜线，表达式匹配的内容会超过注释的结束分隔符(这也是其他解法的问题)。而在 本例中，回溯可能有些令人迷惑，所以读者最好弄明白/x([^/]1[^x]/ )*x/1为什么能匹

配

Years = days /x divide x//365,/x assume non-leap year x/

(可以在空余时间好好想想这个问题。)