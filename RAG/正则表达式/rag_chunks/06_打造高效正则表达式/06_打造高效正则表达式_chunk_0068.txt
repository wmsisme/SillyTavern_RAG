---
章节: 第6章：打造高效正则表达式
章节ID: 06_打造高效正则表达式
切片序号: 68/74
---
引导匹配的工具

A Helping Hand to Guide the Match 看下面的程序：

SCOMMENT=qr{/八*[^*]*\*+(?:[^/*][^*]*\*+)*/};	# 匹配注释

SDOUBLE = qr {"(?:\\.I[^\\"])*"};	# 匹配双引号宇符串

Stext =~s/$DOUBLEISCOMMENT//g;

这里出现了两件新事物。其中之一是表达式 $DOUBLE i SCOMMENTj,它由两个变量组成，都 使用了Perl 特有的qr/…/ 正则表达式“双引号字符串”操作符。我们曾在第3章仔细讨论 过 (101),如果用字符串表示正则表达式，使用字符串的时候必须格外小心。Perl 提供的 qr/ …/运算符解决了这个问题，它会把操作对象 ( operand) 视为正则表达式，但不会实际 应用它。我们在第2章(76)已经看到，这样非常方便。与m/…/ 和 s/…/…/ 一样，我 们可以自己选择分隔符(71),上面使用的是花括号。

另一点是通过用SDOUBLE来匹配双引号字符串。传动装置驱动到$DOUBLE能匹配的位置时， 会一次性匹配整个双引号字符串。这里使用多选分支完全没有问题，因为二者之间并没有

278 第 6 章 ： 打 造 高 效 正 则 表 达 式

重叠。如果我们从左向右扫描这个正则表达式就会发现，应用到字符串时，存在三种可能：

· 注释部分能够匹配，于是一次性匹配注释部分，直接到达注释的末尾，或者…

· 双引号字符串部分能够匹配，于是一次性匹配双引号字符串，直接到达其结尾，或者…

· 上面两者都不能匹配，本轮尝试失败。启动驱动过程，跳过一个字符。